const config = require('./firebase-applet-config.json');

async function getDeploy() {
  // Let's see if we can get a token with npx google-auth-library ? Wait, no.
  // Actually, we don't have authentication to view the raw ruleset directly via REST without a token.
}
